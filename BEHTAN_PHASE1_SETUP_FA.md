# به‌تن — نصب فاز اول کیفیت برنامه و پیگیری

مبنای پچ: `97579cf5e5d16fe77846b02fca56ef66cb526475` از main، با اختلاف کالری ۲۵٪.
این بسته روی سرویس اصلی منتشر نشده و SQL آن روی دیتابیس اصلی اجرا نشده است.

## تغییرات

- سهم هدف وعده‌ها: صبحانه ۲۰٪، میان‌وعده صبح ۱۰٪، ناهار ۳۰٪، عصرانه ۱۰٪، شام ۲۵٪، شب ۵٪. این سهم، هدف الگوریتم است؛ مقدار نهایی با غذا و اندازهٔ واقعی سهم تغییر می‌کند.
- کاهش امتیاز ترکیب‌های تکراری در وعده‌های متوالی، ناهار و شام، میان‌وعده‌های صبح و عصر، و همان وعدهٔ دیروز در صورت وجود گزارش.
- ترجیح میوهٔ کامل و فیبر برای میان‌وعده‌های صبح و عصر؛ ۶ ترکیب در هر دو جایگاه، جمعاً ۱۲ قالب. مقادیر تغذیه‌ای از اقلام موجود کاتالوگ گرفته می‌شود.
- تولید مجدد برای وعده‌های باقی‌مانده؛ وعده‌های مصرف‌شده و مقدارشان حفظ می‌شوند. اگر ترکیب متفاوتی پیدا نشود، پیام روشن نمایش داده می‌شود.
- تفکیک هدف، مجموع برنامه و مصرف ثبت‌شده در خلاصهٔ تغذیه.
- ثبت وزن با اعداد فارسی، روند وزن، بررسی هفتگی پایبندی/گرسنگی/دشواری و نمایش پاسخ‌ها برای ادمین مجاز.
- ثبت نسخهٔ سیاست و شناسهٔ محتوای کاتالوگ در گزارش؛ نگهداری نسخه‌های گزارش برنامه در جدول جداگانه.

## ۱. اعمال پچ در Codespaces

فایل `behtan-meal-quality-progress.patch` را کنار package.json آپلود کن. ابتدا `git status` را ببین؛ اگر ویرایش شخصی داری، آن را جداگانه ذخیره کن. سپس:

```bash
git switch main
git pull --rebase origin main
git switch -c feature/meal-quality-progress
git apply --check behtan-meal-quality-progress.patch
git apply behtan-meal-quality-progress.patch
npm ci
npm run test:experience
npm run test:progress
npm run test:data
npm run test:meal-progress
npm run test:nutrition
npm run test:quality
npm run test:portions
npm run build
npm run build:admin
```

دستورهای تست به Node دارای `--experimental-strip-types` نیاز دارند؛ Node 22.6 یا جدیدتر. اگر `git apply --check` خطا داد، ادامه نده؛ پچ را با force اعمال نکن. در این مرحله فقط فایل‌ها و تست محلی تغییر می‌کنند.

## ۲. آماده‌سازی Supabase پیش از انتشار

روی پروژهٔ درست، با بکاپ موجود و ترجیحاً ابتدا محیط آزمایشی، این سه فایل را **به‌ترتیب و هرکدام یک بار** در SQL Editor اجرا کن:

1. `supabase/migrations/20260921_017_balanced_snacks.sql`
2. `supabase/migrations/20260921_018_progress_journal.sql`
3. `supabase/migrations/20260921_019_plan_revision_history.sql`

نیازمند migrationهای قبلی کاتالوگ، پرداخت، ثبت وعده و پنل ادمین است. فایل seed کامل را روی دیتابیس موجود اجرا نکن. migration 017 صرفاً قالب‌های جدید را درج می‌کند و اقلام غذایی قبلی را بازنویسی نمی‌کند. 018 و 019 جدول و تابع جدید دارند؛ در صورت اجرای موفق قبلی، تکرارشان نکن. برای هر خطای SQL همان‌جا توقف کن.

بررسی نصب:

```sql
select count(*) as new_templates
from public.meal_templates
where id like 'balanced_%';

select to_regclass('public.user_progress_entries') as progress_table,
       to_regclass('public.daily_plan_revisions') as revisions_table;
```

با کاتالوگ استاندارد، شمار قالب‌های جدید ۱۲ است و نام هر دو جدول برمی‌گردد. در نصب با کاتالوگ سفارشی، موجود بودن شناسه‌های غذا در migration را پیش از اجرا بررسی کن. تغییر Edge Function لازم نیست.

## ۳. ثبت و انتشار کد

```bash
git add src scripts supabase package.json BEHTAN_PHASE1_SETUP_FA.md
git diff --cached --stat
git commit -m "Improve meal variety and add progress journal"
git push -u origin feature/meal-quality-progress
```

PR به main بساز:
https://github.com/realSoroush/Behtan-Final/compare/main...feature/meal-quality-progress

بعد از اجرای SQL و موفقیت تست‌ها، PR را merge کن و Deploy اپ و ادمین را در Vercel بررسی کن. فایل پچ و ZIP را commit نکن.

## ۴. چک بعد از انتشار با حساب تست

1. خلاصهٔ هدف/برنامه/مصرف را بررسی کن؛ هدف پروتئین ممکن است با مجموع غذاها کمی فرق داشته باشد و اکنون برچسب آن روشن است.
2. یک وعده را مصرف‌شده علامت بزن. تولید مجدد و سپس رفرش کن؛ آن وعده و مقدارش باید حفظ شده باشد. تیک را بردار و دوباره بررسی کن.
3. میان‌وعده‌ها و ناهار/شام را بررسی کن؛ با رژیم محدود، نبود قالب مجاز می‌تواند تنوع را محدود کند.
4. در «روند من»، وزن و پاسخ‌های هفته را ذخیره کن؛ رفرش و ویرایش ثبت امروز را امتحان کن.
5. در پنل ادمین، همان کاربر را باز کن و پاسخ‌ها را ببین. کاربر عادی نباید به پاسخ دیگران دسترسی داشته باشد.
6. ظاهر موبایل و تم روشن/تیره را بررسی کن.

## مرزهای این فاز

- فرمول کالری ۲۵٪، سقف اختلاف کالری و فرمول هدف ماکروها تغییر نکرده‌اند.
- تنوع و میوه/فیبر ترجیح الگوریتم‌اند؛ برای عبور از محدودیت آلرژی، اندازهٔ سهم یا کنترل ماکرو استفاده نمی‌شوند. حذف کامل تکرار در همهٔ کاتالوگ‌های محدود تضمین نمی‌شود.
- هنگام وجود غذای مصرف‌شده، واقعیت مصرف حفظ می‌شود؛ اگر مجموع روز با هدف هماهنگ نشود، پیام و اعداد واقعی نمایش داده می‌شود.
- تاریخچهٔ دیروز به وجود گزارش ثبت‌شدهٔ دیروز وابسته است. قطع دریافت تاریخچه، کنترل تکرار همان روز را متوقف نمی‌کند.
- ثبت وزن هدف کالری یا وزن پروفایل را خودکار تغییر نمی‌دهد؛ بازبینی خودکار تغذیه‌ای در این فاز اضافه نشده است.
- رابط آخرین ۹۰ ثبت را نشان می‌دهد؛ داده‌های قدیمی‌تر از دیتابیس حذف نمی‌شوند. رابط فقط ثبت امروز را ویرایش می‌کند.
- آرشیو نسخه‌های برنامه از نوشتن‌های بعد از migration شروع می‌شود؛ نسخهٔ موجود هنگام اولین به‌روزرسانی حفظ می‌شود. رابط نمایش آرشیو هنوز اضافه نشده است.
- گزارش‌ها خروجی مرورگرند؛ شناسهٔ محتوا امضای امنیتی یا تأیید مستقل پزشکی نیست.
- فرمول‌ها و داده‌های غذایی موجود بازبینی بالینی جدید نشده‌اند.

## برگشت تغییرات

برای برگشت اپ، commit این PR را revert کن و نسخهٔ قبلی را Deploy کن. جدول روند و آرشیو را حذف نکن تا اطلاعات ثبت‌شده از بین نرود. در صورت نیاز به غیرفعال‌کردن قالب‌های جدید، ابتدا مصرف آن‌ها و ستون فعال‌بودن کاتالوگ را بررسی کن؛ پاک‌کردن داده‌های کاربران لازم نیست.

## ادامهٔ مسیر محصول

پس از پایلوت این بسته: اندازه‌گیری مسیر ثبت‌نام تا پرداخت و استفادهٔ روزانه، ترجیحات واقعی تهیهٔ غذا و هزینه، سپس طراحی بازبینی برنامه بر مبنای روند و بررسی متخصص. این‌ها در این پچ پیاده‌سازی نشده‌اند.
