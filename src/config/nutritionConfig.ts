/**
 * Nutrition diagnostics toggle.
 * Keep this true while validating the MVP. It only logs formula inputs/outputs
 * to the user's browser console; no phone, name or image data is included.
 */
export const NUTRITION_DEBUG_CONFIG = {
  isActive: true,
} as const;
